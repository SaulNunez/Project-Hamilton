using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

/// <summary>
/// Future work, handles updating UI state with hub config in lobby to allow setup
/// </summary>
public class LobbyOptionsUiConnections : MonoBehaviour
{
    [SerializeField]
    Slider impostorSlider;

    [SerializeField]
    Slider reunionCooldown;

    [SerializeField]
    Slider sabotageDuration;

    [SerializeField]
    Slider actionDistance;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
